<h1>New Activity</h1>

<!-- <%= render 'form', activity: @activity %> -->

<%= simple_form_for(@activity) do |f| %>
<%= f.input :name %>
<%= f.input :content %>
<%= f.association :things, as: :check_boxes %>
<%= f.button :submit %>
<% end %>



<%= link_to 'Back', activities_path %>
